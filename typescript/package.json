{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "compute-lcm": "^1.1.0",
    "gts": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^10.0.3",
    "gts": "^1.1.2",
    "typescript": "~3.7.0"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  }
}
